using System;
using System.Runtime.CompilerServices;

namespace %namespace%
{
    [CompilerGenerated]
    public static class Program
    {
        private static readonly byte[] Memory = new byte[%memorylimit%];
        private static int CellCounter = 0;

        internal static void MultiplyAdd(int offset, int value)
        {
            int result = Memory[CellCounter] * value;
            Memory[CellCounter + offset] = (byte)result;
        }

        public static void Main(string[] args)
        {
%code%
        }
    }
}